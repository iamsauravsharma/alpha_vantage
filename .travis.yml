language: rust

rust:
  - stable
  - beta
  - nightly

sudo: true

git:
  depth: false
 
dist: xenial
  
os:
  - linux
  - osx
  - windows

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then
      sudo apt-get update;
    fi

addons:
  apt:
    packages:
      - libcurl4-openssl-dev
      - libelf-dev
      - libdw-dev
      - cmake
      - gcc
      - binutils-dev
      - libiberty-dev

install:
  - rustup component add rustfmt || RUSTFMT="no"
  - rustup component add clippy || CLIPPY="no"

before_script:
  - if [[ ! "RUSTFMT" == "no" ]]; then
      rustfmt -V;
    fi
  - if [[ ! "CLIPPY" == "no" ]]; then
      cargo clippy -V;
    fi

script:
  - if [[ ! "RUSTFMT" == "no" ]]; then
      cargo fmt --verbose --all -- --check;
    fi
  - if [[ ! "CLIPPY" == "no" ]]; then
      cargo clippy --verbose --all -- -D warnings;
    fi
  - cargo build --verbose --all
  - cargo doc --no-deps --all
  - cargo test --verbose --all

before_deploy:
  - chmod +x scripts/codecov_coverage.sh
  - chmod +x scripts/create_index.sh

deploy:
  - provider: script
    script: bash scripts/create_index.sh
    skip-cleanup: true
    on:
      branch: master
      condition: $TRAVIS_OS_NAME = linux && $TRAVIS_RUST_VERSION = stable
  - provider: script
    script: bash scripts/codecov_coverage.sh
    skip-cleanup: true
    on:
      condition: $TRAVIS_OS_NAME = linux && $TRAVIS_RUST_VERSION = stable
  - provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    keep-history: true
    local-dir: "target/doc"
    on:
      branch: master
      condition: $TRAVIS_OS_NAME = linux && $TRAVIS_RUST_VERSION = stable